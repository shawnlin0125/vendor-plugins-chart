# 此檔案由 scan-plugins.sh 自動維護，勿手動編輯 plugins 清單
# GitHub Repo: https://github.com/shawnlin0125/vendor-plugins-chart

namespace: vendor-staging

# scan-plugins.sh fills this list automatically
plugins: []

registry:
  server: ghcr.io
  imagePullSecrets:
    - name: image-registry
  # Images are pushed as ghcr.io/shawnlin0125/vendor-plugin-<name>:<tag>
