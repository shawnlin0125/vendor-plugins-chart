# 此檔案由 scan-plugins.sh 自動維護，勿手動編輯 plugins 清單
# GitHub Repo: https://github.com/shawnlin0125/vendor-plugins-chart

namespace: vendor-staging

plugins:
  - name: demo
    image: ghcr.io/shawnlin0125/vendor-plugin-demo
    tag: latest
    port: 8080

registry:
  server: ghcr.io
  imagePullSecrets:
    - name: image-registry

# Ingress config — override in values-staging.yaml
ingress:
  enabled: false
  host: vendor-demo.shawnlin.online
